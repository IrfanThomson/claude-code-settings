# Sensitive data
*.key
*.pem
*credentials*
*.env
secrets.env
secrets.env.dec

# Encrypted secrets (keep this safe elsewhere!)
# Uncomment if you want to store encrypted secrets in repo:
# !secrets.env.enc

# Session data
history.jsonl
debug/
file-history/
plans/
session-env/
shell-snapshots/
todos/
telemetry/
statsig/

# Cache and temporary files
*.log
*.tmp
cache/
marketplaces/
repos/

# Backups
*.backup
*.backup.*

# Project-specific (not global settings)
projects/

# Plugin binaries and downloaded repos
plugins/cache/
plugins/marketplaces/
plugins/repos/
plugins/installed_plugins_v2.json
